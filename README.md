# CertiPlan Premium

Static marketing website for CertiPlan Services Ltd.

## Publishing

The site is designed to be published from the repository root with GitHub Pages.
The entry point is `index.html`.

## Local preview

Open `index.html` directly in a browser, or serve this directory with any static web server.

